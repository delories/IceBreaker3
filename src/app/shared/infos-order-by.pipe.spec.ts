import { InfosOrderByPipe } from './infos-order-by.pipe';

describe('InfosOrderByPipe', () => {
  it('create an instance', () => {
    const pipe = new InfosOrderByPipe();
    expect(pipe).toBeTruthy();
  });
});
